import { ComponentFixture, TestBed } from '@angular/core/testing';
import { RepartidorhomepagePage } from './repartidorhomepage.page';

describe('RepartidorhomepagePage', () => {
  let component: RepartidorhomepagePage;
  let fixture: ComponentFixture<RepartidorhomepagePage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(RepartidorhomepagePage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
