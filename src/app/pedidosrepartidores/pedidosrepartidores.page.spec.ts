import { ComponentFixture, TestBed } from '@angular/core/testing';
import { PedidosrepartidoresPage } from './pedidosrepartidores.page';

describe('PedidosrepartidoresPage', () => {
  let component: PedidosrepartidoresPage;
  let fixture: ComponentFixture<PedidosrepartidoresPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(PedidosrepartidoresPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
